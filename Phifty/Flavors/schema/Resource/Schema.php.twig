<?php
namespace {{namespace}}\Model;
use Maghead\Schema\DeclareSchema;

class {{schemaName}} extends SchemaDeclare
{
    function schema() {
{% for column in schemaColumns %}
{% if column.var %}
        $this->column('{{ column.name }}')
            ->{{ column.type }}({{ column.var }});
{% else %}
        $this->column('{{ column.name }}')
            ->{{ column.type }}();
{% endif %}
{% endfor %}
    }
}
